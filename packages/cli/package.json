{
  "name": "bridgehound-cli",
  "version": "0.0.1",
  "description": "Command-line interface for bridgehound.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "bridgehound": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "bridgehound": "workspace:*",
    "cac": "^6.7.0",
    "kleur": "^4.1.0",
    "ora": "^8.0.0"
  }
}
